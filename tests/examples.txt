npx playwright codegen --device "DeviceName"
^ automatic code generator

toHaveURL - page toHaveURL
toHaveTitle - page title
toBeVisible - element visible
toBeEnabled / toBeDisabled - disabled status of element (i.e button)
toBeChecked - radio input checked status
toHaveAttribute - element has attribute
toHaveText - element matches text
toContainText - element contains text
toHaveValue - input has value
toHaveCount - list of elements has given length

negative assertion - expect(locator).not.toHaveAttribute()

soft assertion - expect.soft(locator).toHaveAttribute()
Doesn't end tests

more - https://playwright.dev/docs/test-assertions

selecting option from dropdown
await page.locator('selectelement').selectOption({label: x})
({value :x})
({index :x})
("Text") visible text
select multiple
page.selectOption('selectElement', ['x','y','z']) array of visible text on options

 npx playwright test frame.test --project chromium --headed